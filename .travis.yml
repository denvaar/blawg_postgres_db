language: elixir

elixir:
  - 1.8

otp_release:
  - 22.0

script:
  - mix do local.hex --force
  - mix deps.get
  - mix test
  - travis_wait mix dialyzer --halt-exit-status

cache:
  directories:
    - priv/plts
